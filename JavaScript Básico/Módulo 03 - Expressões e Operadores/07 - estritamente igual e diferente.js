let one = 1
let two = 2

// === Estritamente igual a (ele verifica tipo e valor)
console.log(one === 1)
console.log(one === '1')

// !== Estritamente diferente de
console.log( one !== two)
console.log(one !== 1)
console.log(one !== '1')